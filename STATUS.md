# Basketball Analysis System — 現状まとめ

## 成果物一覧

### 動画（現行パイプライン出力）

| ファイル | 内容 | サイズ |
|---------|------|--------|
| `outputs/sam3_300s/full_detection_30s.mp4` | 選手セグメント（チームカラー）+ SAM3ボール + コートライン + ミニマップ（30秒） | 50MB |
| `outputs/sam3_300s/full_detection_300s.mp4` | 同上 300秒フル | 556MB |
| `outputs/sam3_300s/tactical_map_300s.mp4` | ブロードキャスト + フルコート俯瞰マップ サイドバイサイド（300秒） | 368MB |

### 画像・データ

| ファイル | 内容 |
|---------|------|
| `outputs/shot_player/shot_chart_frame_300s.png` | シュート検出結果（各シュートのフレーム画像） |
| `outputs/shot_player/shot_chart_all_300s.png` | 全シュート位置の重ね合わせ |
| `outputs/shot_player/shots_300s.json` | シュート検出データ（57本） |
| `outputs/annotations.json` | 手動アノテーション（16フレーム）|

---

## 現行パイプライン構成

```
動画入力
  │
  ├─ pipeline/ball_tracker_sam3.py        SAM3によるボール追跡（93% / 8370フレーム）
  │
  ├─ pipeline/generate_detection_video.py  選手検出・追跡・コートライン・ミニマップ動画出力
  │    ├─ YOLOv8n-seg            選手セグメント（チームカラー塗りつぶし）
  │    ├─ ByteTrack              ID付き継続追跡
  │    ├─ HSV                    ユニフォーム色でチーム分類
  │    └─ OpticalFlowTracker     手動アノテーション→LK光学流量→90f毎ハリス角自動補正
  │
  ├─ pipeline/generate_tactical_map.py     ブロードキャスト＋フルコート俯瞰マップ動画出力
  │
  └─ pipeline/shot_player_analysis.py      SAM3ボール軌跡の放物線フィットでシュート検出

コート校正（手動）
  calibration/annotator.py / calibration/annotator_web.py / calibration/court_review.py
  └─ calibration/compute_homography.py → outputs/annotations.json
```

---

## 機能別まとめ

### ボール追跡（SAM3）
- 300秒中 **93%**（8370/8991フレーム）でボール位置取得
- 結果は `ball_positions_sam3_300s.json` に保存済み

### 選手検出・追跡
- YOLOv8n-seg でフレームごとに検出（セグメントマスク付き）
- ByteTrack でID付き継続追跡（最大10人）
- ユニフォーム色（HSV）でチーム分類（白チーム / 紺チーム）

### コートライン描画
- 手動アノテーション 16点（最初の70秒分）でホモグラフィー初期推定
- Lucas-Kanade 光学流量でフレーム間追跡
- 90フレーム（約3秒）ごとに白線コーナー検出で自動再キャリブレーション

### シュート検出
- SAM3ボール軌跡の放物線フィット（R² > 0.70）でシュート動作を自動検出
- 300秒で **57本**検出
- タイムスタンプ・ピクセル座標・軌跡高さをJSONで保存

---

## 精度上の限界

| 課題 | 詳細 |
|------|------|
| **コート座標のズレ** | アノテーションが最初の70秒分のみ。残り230秒は自動補正頼みで精度が低下する |
| **ファーハーフの選手** | エンドラインカメラのためハーフコート以降の選手は追跡不可 |
| **シュート成否判定** | 入ったか外れたかは判定していない（軌跡のみ検出） |
| **チーム誤分類** | ユニフォーム色が似ている場合や遠距離で精度が落ちる |

---

## 次のステップ：顧問向けキャリブレーション設計

### 背景と方針

実際の高校部活顧問に使ってもらうには、以下の課題がある：

1. **体育館・撮影位置ごとにコートの見え方が全く異なる**（広角/狭角、エンドライン/サイドライン寄り、コート全面/ハーフのみ/センターのみ）
2. 毎回 AI で自動検知するのはこの条件変動に対して脆弱すぎる
3. **1会場 × カメラ位置で1度だけキャリブレーション**し、プロファイルとして保存・使い回すのが現実的

### 画角パターン別の対応方針

| 画角 | 使えるランドマーク | 精度 | 出力できるスタッツ |
|------|------------------|------|-----------------|
| **片方ゴール寄り**（推奨） | ベースライン角・ペイント4角・FTライン・3Pアーク（6〜8点） | 高 | ショットチャート・ヒートマップ・移動距離 |
| **センターのみ** | サイドライン×センターライン交点・センターサークル（3〜4点） | 低 | 通過方向・テンポのみ（座標なし） |
| **フルコート** | 隅4点＋内部ライン交点（8点以上） | 高 | フルコートポジション |

### 実装する「会場キャリブレーションUI」の仕様

```
calibrate.py  ← 新規作成予定

1. 動画から任意フレームを取得（最初の5秒から選択）
2. 画面左：映像フレーム、画面右：FIBAコート見取り図（名前付きランドマーク20〜30点）
3. 顧問が「映っているランドマークだけ」をクリックしてラベル付与
4. cv2.findHomography（RANSAC付き）でH算出
5. コートラインを映像に逆投影オーバーレイで目視確認
6. OKなら「会場名 × カメラ位置」プロファイルとして保存
7. 以降その設定の動画はプロファイルを読み込んで処理
```

**信頼領域ゲート:** クリックしたランドマークの凸包の外にいる選手は座標を「範囲外」とフラグし、数値を出力しない（外挿エラーを防ぐ）。

### 作る順番

1. `calibration/calibrate.py` — 会場プロファイル作成UI（クリック式、信頼領域付き）
2. コート多角形による幾何フィルタ（コート外の誤検出を除去、学習不要）
3. 出力テンプレート3点（ハイライト動画 / スタッツ1枚 / ヒートマップ）
4. Active Learning ループ（怪しいフレームだけ顧問が修正 → バッチ学習）

---

## 試したがうまくいかなかったこと

詳細は [`docs/DEPRECATED.md`](docs/DEPRECATED.md) を参照。

| アプローチ | 問題 |
|-----------|------|
| KaliCalib（専用コート検知モデル） | 平均誤差 400〜474px。ブロードキャストカメラ向けのモデルでエンドライン画角に非対応 |
| run_hsv_only_v18〜v22（HSV パイプライン） | コート検出が照明・カメラ条件で不安定。SAM3方式に完全置き換え |
| YOLOv8-Pose コート検知 | 体育館照明・エンドライン画角で安定しない |
| ball_tracker_sam2 / yoloworld | SAM3より追跡率・速度が劣る |

---

*最終更新: 2026-06-03*
